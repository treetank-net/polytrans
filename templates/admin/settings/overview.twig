<div class="wrap">
    <h1>{{ __('TreeTank Translation Workflows', 'treetank-trans')|esc_html }}</h1>

    <div class="card">
        <h2>{{ __('Welcome to TreeTank Translation Workflows', 'treetank-trans')|esc_html }}</h2>
        <p>{{ __('This plugin is a translation automation tool that helps you manage multilingual content.', 'treetank-trans')|esc_html }}</p>

        <h3>{{ __('Quick Links', 'treetank-trans')|esc_html }}</h3>
        <ul>
            <li>
                <a href="{{ admin_url('admin.php?page=polytrans-execute-workflow')|esc_url }}">
                    {{ __('Execute Workflow', 'treetank-trans')|esc_html }}
                </a>
                - {{ __('Run translation workflows on your posts', 'treetank-trans')|esc_html }}
            </li>
            <li>
                <a href="{{ admin_url('admin.php?page=polytrans-workflows')|esc_url }}">
                    {{ __('Manage Workflows', 'treetank-trans')|esc_html }}
                </a>
                - {{ __('Create and edit post-processing workflows', 'treetank-trans')|esc_html }}
            </li>
            <li>
                <a href="{{ admin_url('admin.php?page=polytrans-tag-translation')|esc_url }}">
                    {{ __('Tag Translations', 'treetank-trans')|esc_html }}
                </a>
                - {{ __('Manage tag translations', 'treetank-trans')|esc_html }}
            </li>
            {% if can_manage_options %}
                <li>
                    <a href="{{ admin_url('admin.php?page=polytrans-settings')|esc_url }}">
                        {{ __('Settings', 'treetank-trans')|esc_html }}
                    </a>
                    - {{ __('Configure the plugin (Admin only)', 'treetank-trans')|esc_html }}
                </li>
                <li>
                    <a href="{{ admin_url('admin.php?page=polytrans-logs')|esc_url }}">
                        {{ __('Logs', 'treetank-trans')|esc_html }}
                    </a>
                    - {{ __('View system logs (Admin only)', 'treetank-trans')|esc_html }}
                </li>
            {% endif %}
        </ul>

        <h3>{{ __('How to Use', 'treetank-trans')|esc_html }}</h3>
        <ol>
            <li>{{ __('Edit any post in WordPress', 'treetank-trans')|esc_html }}</li>
            <li>{{ __('Look for the "Translation Workflows" meta box in the sidebar', 'treetank-trans')|esc_html }}</li>
            <li>{{ __('Click "Execute" on any workflow to process your post', 'treetank-trans')|esc_html }}</li>
        </ol>
    </div>
</div>

